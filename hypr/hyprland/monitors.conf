# Monitor Configuration
# This file handles different monitor setups per machine

# Fallback - auto-detect everything (safe default)
monitor = , preferred, auto, 1

# Desktop (lexnaid-pc) - dual monitor setup
# Uncomment/modify these on desktop, or use host-specific config
# monitor = HDMI-A-1, 2560x1440@144, 0x0, 1
# monitor = HDMI-A-2, preferred, auto, 1

# Laptop - internal display
# monitor = eDP-1, preferred, auto, 1
# For external monitor when docked:
# monitor = HDMI-A-1, preferred, auto-right, 1
