# Default host config (fallback)
# Create a file named after your hostname for machine-specific settings
# Example: ~/.config/hypr/hosts/lexnaid-pc.conf for desktop

# This file is intentionally minimal - monitors auto-detect by default
